# stockmind.kr 연결 후 할 일 (순서대로)

도메인을 Cloudflare Pages에 연결하셨으니, 아래를 위에서부터 하나씩 하시면 됩니다.

---

## ① 코드의 도메인 주소 교체 (완료됨)

`index.html`, `sitemap.xml`, `robots.txt` 안의 옛 주소(auto-shorts.pages.dev)를
전부 `https://stockmind.kr`로 바꿔뒀습니다. **이 파일들을 저장소에 덮어쓰고 push하면 적용**됩니다.

---

## ② https 확인 (필수)

검색엔진은 `https://`가 필수입니다(http는 감점). Cloudflare Pages는 https를 자동 제공해요.

- 브라우저에서 **https://stockmind.kr** 로 접속 → 자물쇠 아이콘(보안 연결) 뜨는지 확인
- Cloudflare 대시보드 → 해당 도메인 → **SSL/TLS** → 모드가 **Full (Strict)** 인지 확인
- `http://stockmind.kr` 로 들어가도 자동으로 `https://` 로 넘어가는지 확인
  (안 넘어가면: SSL/TLS → Edge Certificates → **Always Use HTTPS** 켜기)

---

## ③ 주소 하나로 통일 (www 처리)

검색엔진은 `stockmind.kr` 와 `www.stockmind.kr` 를 **다른 사이트로 인식**합니다.
둘 다 열리면 SEO 점수가 분산되니, 하나로 몰아줘야 해요. (코드는 www 없는 주소로 맞춰뒀어요.)

- Cloudflare 대시보드 → 도메인 → **Rules → Redirect Rules → Create rule**
- 이름: `www to apex`
- 조건: Hostname `equals` `www.stockmind.kr`
- 동작: **Dynamic redirect** → `concat("https://stockmind.kr", http.request.uri.path)` , 상태 **301**
- 저장. 이제 www로 와도 www 없는 주소로 넘어갑니다.

> 잘 모르겠으면 이 단계는 건너뛰어도 사이트는 작동합니다. 다만 SEO엔 해두는 게 좋아요.

---

## ④ 배포 후 필수 확인 3가지

브라우저에서 아래 3개 주소를 직접 열어보세요:

1. **https://stockmind.kr/** → 사이트가 뜨는가
2. **https://stockmind.kr/robots.txt** → 텍스트가 뜨고 `Sitemap: https://stockmind.kr/sitemap.xml` 이 보이는가
3. **https://stockmind.kr/sitemap.xml** → XML이 뜨고 loc 주소가 stockmind.kr 인가

셋 다 정상이면 검색엔진 등록으로 넘어갑니다.

---

## ⑤ 검색엔진에 새 도메인 등록 (3곳)

기존에 auto-shorts.pages.dev로 등록했더라도, **새 도메인은 별개**라 다시 등록해야 해요.

### 네이버 서치어드바이저
1. 웹마스터도구 → 사이트 추가 → `https://stockmind.kr` 등록 → 소유권 확인
   (Cloudflare Pages는 HTML 태그 방식이 편함. 네이버가 주는 meta 태그를 index.html `<head>`에 추가)
2. **요청 → 사이트맵 제출**: `https://stockmind.kr/sitemap.xml`
3. **요청 → 웹페이지 수집**: `https://stockmind.kr` 입력 후 요청
4. **검증 → robots.txt**: 수집요청

### 구글 서치콘솔 (search.google.com/search-console)
1. 속성 추가 → `https://stockmind.kr` → 소유권 확인
2. **Sitemaps** 메뉴 → `sitemap.xml` 제출
3. 상단 URL 검사창에 `https://stockmind.kr` → **색인 요청**

### 빙 웹마스터도구 (bing.com/webmasters)
- 구글 서치콘솔에서 **Import** 하면 5분 만에 완료
- 빙에 뜨면 ChatGPT 검색에도 노출돼서 요즘 중요함

---

## ⑥ 네이버 소유권 meta 태그 넣는 법 (참고)

네이버가 `<meta name="naver-site-verification" content="...">` 같은 태그를 줄 거예요.
그걸 `index.html`의 `<head>` 안(다른 meta 태그들 근처)에 붙여넣고 다시 push하면 됩니다.
붙여넣을 위치를 모르면 그 태그를 저에게 알려주세요. 넣어드릴게요.

---

## ⏱️ 기대치

- 네이버: 수집까지 **약 14~16일**
- 구글·빙: 통상 **1~4주**
- 즉, 오늘 등록해도 검색에 바로 안 떠요. 그동안 블로그·유튜브로 직접 유입을 만드는 게 중요합니다
  (→ 검색노출_홍보가이드.md 참고)

---

## 다음에 도움 드릴 수 있는 것

- 네이버/구글 소유권 meta 태그를 주시면 코드에 넣어드림
- 빈터공간 블로그용 사이트 소개글 작성
- www 리다이렉트 규칙 세부 설정 안내
